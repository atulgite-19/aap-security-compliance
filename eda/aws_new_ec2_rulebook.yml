---
sources:
  - aws.cloudwatch:
      filters:
        - detail.state: ["running"]

rules:
  - name: New EC2 Instance Detected
    condition: event["detail-type"] == "EC2 Instance State-change Notification"
    action:
      run_job_template:
        name: "AWS Security Compliance Workflow"
        organization: "AWS-Automation"
